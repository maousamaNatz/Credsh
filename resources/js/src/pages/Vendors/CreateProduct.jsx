import React from 'react';
import Layout from '../Layouts/Layout';
import { Link, LinkButton, Button } from '../common/buttons';

export default function HomePage({ popularVendors, latestArticles }) {
    return (
        <Layout>

        </Layout>
    )
}
